import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class CreateGame extends Component {
  static propTypes = {
    prop: PropTypes,
  }

  render() {
    return <div>Creating a Game</div>
  }
}
